<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.bits.frenchify">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Frenchify">
        <activity android:name=".CategoriesOfLearning"></activity>
        <activity android:name=".Learning" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".Login" />
        <activity
            android:name=".LearningOrAssessment"
             />
        <activity android:name=".Signup" />
        <activity android:name=".SplaceScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>